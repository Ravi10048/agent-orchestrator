# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Data / SQLite — ANY path (the dev DB can hold tokens from past runs; never commit it)
*.db
*.db-journal
*.db-wal
*.db-shm
backend/tmp/

# Env / secrets
.env
*.env
!.env.example

# Internal — not for the public repo (employer/clean-room notes, Claude workspace guide)
CLAUDE.md
PROJECT_STATUS.md

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# TypeScript / Vite build artifacts (generated by `tsc -b`)
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# Docs site build (if mkdocs runs here)
site/

# OS / editor
.DS_Store
.vscode/
.idea/

# local dev (start.sh/stop.sh) logs + pids
.run/
